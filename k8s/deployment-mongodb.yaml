kind: Deployment
metadata:
  name: relay-mongodb
  namespace: relay
spec:
  replicas: 1
  selector:
    matchLabels:
      app: relay-mongodb
  template:
    metadata:
      labels:
        app: relay-mongodb
    spec:
      containers:
        - name: relay-mongodb
          image: mongo:latest
          ports:
            - containerPort: 27017